# todolist
This is a simple To-Do List application built with pure JavaScript. It allows users to add, update, delete, and mark tasks as completed. The app stores all tasks in localStorage, so your to-do list is saved even if you close or reload the browser.

